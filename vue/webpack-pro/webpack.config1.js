/*
let path = require('path');
// node中解析路径，可以把一个相对路径转化绝对路径
module.exports = {
  //入口文件 会将入口中所有依赖的结果打包到出口中
  entry:'./src/main.js',
  output:{
      // 打包后文件的名字
      filename:'bundle.js',
      // 打包后的结果必须是绝对路径
      path:path.resolve('./dist')
  }
};*/
